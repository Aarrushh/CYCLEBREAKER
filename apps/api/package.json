{
  "name": "@cyclebreaker/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cyclebreaker/shared": "0.0.0",
    "@fastify/cors": "^8.4.1",
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^16.4.5",
    "fastify": "^4.27.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.2"
  }
}
